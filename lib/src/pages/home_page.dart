import 'package:flutter/material.dart';

class HomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Fitness"),
      ),
      body: Center(
        child: Text("copy ur design into this file"),
      ),
    );
  }
}
